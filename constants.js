const path = require('path');

module.exports = {
    CAMERA_FILE_PATH: path.join(__dirname, 'images'),
    TMP_DIR: path.join(__dirname, 'images', 'tmp'),
    TIMELAPSE_DIR: path.join(__dirname, 'timelapse')
};